//maintest.ts
// This is a simple test file for Vitest.
// It includes a basic test case that checks if 1 + 1 equals 2.
import { describe, it, expect } from 'vitest';

// This is a simple test file for Vitest.
// It includes a basic test case that checks if 1 + 1 equals 2.
// You can add more complex tests as needed.
describe('Sample Test Suite', () => {
  it('adds two numbers', () => {
    expect(1 + 1).toBe(2);
  });
});
